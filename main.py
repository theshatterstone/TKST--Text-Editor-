from tkinter import *
root = Tk()
root.title('TKST text editor')
#all the code goes here
Label(root, text='This is the area, where I will build a text editor.').grid(row=0, column=0, sticky='e') #filler label, so the window isn't empty


root.mainloop()